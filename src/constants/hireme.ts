//This is for the social media links

const x = { route: '', label: 'X' };
const linktr = { route: '', label: 'Linktr' };
const links = [x, linktr];

export default class HIREME {
  static readonly links = links;
  static readonly x = x;
  static readonly linktr = linktr;
}
